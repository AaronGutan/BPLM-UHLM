// Заполняет список объектов сверки по фиксированному перечню объектов БП.
// Используется, если состав плана обмена "Консолидация" еще не загружен в метаданные.
//
Процедура ЗаполнитьСписокОбъектовПоСпискуБП(ВыбранныеРанее, СтатусСверки)

	МетаданныеОбъекта = Метаданные.Документы.Найти("АвансовыйОтчет");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("АвизоМПЗВходящее");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("АвизоМПЗИсходящее");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("АвизоОСВходящее");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("АвизоОСИсходящее");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("АвизоПрочееВходящее");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("АвизоПрочееИсходящее");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("АвизоРасчетыВходящее");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("АвизоРасчетыИсходящее");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("АктОбОказанииПроизводственныхУслуг");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("АктПроверкиСтраховыхВзносов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("БольничныйЛист");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ВводНачальныхОстатков");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ВозвратМатериаловИзЭксплуатации");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ВозвратОСОтАрендатора");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ВозвратТоваровОтПокупателя");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ВозвратТоваровПоставщику");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ВосстановлениеНДС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ВосстановлениеНДСПоОбъектамНедвижимости");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ВыдачаДенежныхДокументов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ВыкупПредметовЛизинга");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ВыплатыСамозанятым");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ГТДИмпорт");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ДепонированиеЗарплаты");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ЗавершениеРемонтаОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ЗаявлениеОВвозеТоваров");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ЗаявлениеОЗачетеВСчетПредстоящейОбязанности");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ИзменениеУсловийАренды");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("КомплектацияНоменклатуры");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("КорректировкаДолга");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("КорректировкаЕНС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("КорректировкаПоступления");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("КорректировкаРеализации");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("МодернизацияОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("НачислениеДивидендов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("НачислениеЗарплаты");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("НачислениеНДСпоСМРхозспособом");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("НачислениеОценочныхОбязательствПоОтпускам");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("НачислениеПеней");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОбесценениеВнеоборотныхАктивов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОказаниеУслуг");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОперацияБух");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОперацияНалоговогоУчетаПоНДФЛ");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОперацияПоЕдиномуНалоговомуСчету");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОперацияСБилетом");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОперацияСИнвестиционнымВычетом");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОперацияСПатентом");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОплатаПлатежнойКартой");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОприходованиеТоваров");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("Отпуск");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОтражениеЗарплатыВБухучете");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОтражениеЗарплатыВУчете");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОтражениеНачисленияНДС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОтражениеНДСКВычету");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОтчетКомиссионераОПродажах");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОтчетКомитентуОПродажах");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОтчетОператораСистемыПлатон");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОтчетОРозничныхПродажах");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ОтчетПроизводстваЗаСмену");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПереводОСВМалоценноеОборудование");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПередачаЗадолженностиНаФакторинг");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПередачаМатериаловВЭксплуатацию");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПередачаНМА");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПередачаОборудованияВМонтаж");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПередачаОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПередачаОСВАренду");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПередачаТоваров");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПеремещениеОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПеремещениеТоваров");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПереоценкаТоваровВРознице");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПерерасчетНДФЛ");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПерерасчетСтраховыхВзносов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПересортицаТоваров");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПодготовкаКПередачеОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПодтверждениеНулевойСтавкиНДС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПоступлениеВАренду");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПоступлениеДенежныхДокументов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПоступлениеДопРасходов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПоступлениеИзПереработки");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПоступлениеНаРасчетныйСчет");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПоступлениеНМА");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПоступлениеТоваровУслуг");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПрекращениеАренды");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("Премия");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПринятиеКУчетуНМА");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПринятиеКУчетуОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПриходныйКассовыйОрдер");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ПутевойЛист");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("РаспределениеНДС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("РасходныйКассовыйОрдер");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("РасходыПредпринимателя");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("РеализацияОтгруженныхТоваров");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("РеализацияТоваровУслуг");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("РеализацияУслугПоПереработке");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("РегистрацияОблагаемойБазыРуководителя");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("РегламентнаяОперация");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("РезервыПоОплатеТруда");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СведенияОбУдержанномНДФЛ");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СписаниеДепонированнойЗарплаты");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СписаниеМатериаловИзЭксплуатации");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СписаниеНДС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СписаниеНМА");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СписаниеОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СписаниеСРасчетногоСчета");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СписаниеТоваров");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СправкиНДФЛДляПередачиВНалоговыйОрган");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СчетФактураВыданный");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("СчетФактураПолученный");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ТребованиеНакладная");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("УведомлениеОбИсчисленныхСуммахНалогов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("УведомлениеОбОстаткахПрослеживаемыхТоваров");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("УведомлениеОбУменьшенииНалогаПоПатенту");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("Увольнение");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("УсловияОплатыПоступления");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ФормированиеЗаписейКнигиПокупок");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ФормированиеЗаписейКнигиПродаж");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Документы.Найти("ФормированиеУставногоКапитала");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Документ", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("БанковскиеСчета");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Билеты");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("БланкиСтрогойОтчетности");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Валюты");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ВидыВзаиморасчетов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ВидыВычетовНДФЛ");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ВидыДокументовФизическихЛиц");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ВидыДоходовНДФЛ");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ВидыДоходовПоСтраховымВзносам");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ВидыНалоговИПлатежейВБюджет");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ВидыОплатОрганизаций");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ВидыРемонтовОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ВидыТарифовСтраховыхВзносов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ВыплатыСамозанятымПрисоединенныеФайлы");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ГодовыеГрафикиАмортизацииОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Гостиницы");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ДенежныеДокументы");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ДоговорыКонтрагентов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Должности");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ДоходыБудущихПериодов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("КлассификаторЕдиницИзмерения");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("КлассификаторОКПД2");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("КлассификаторТНВЭД");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("КодыОперацийПрослеживаемости");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("КодыОперацийРаздела7ДекларацииПоНДС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Контрагенты");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("КонтрольныеЗнакиГИСМ");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("МедицинскиеОрганизации");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("НазначениеЦелевыхСредств");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("НазначенияИспользованияНМА");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("НазначенияИспользованияНоменклатуры");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("НаправленияИспользованияПрибыли");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("НематериальныеАктивы");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Номенклатура");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("НоменклатурныеГруппы");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("НомераГТД");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ОбъектыВыполненияРабот");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ОбъектыСтроительства");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Организации");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ОснованияПраваПодписи");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ОснованияУвольнения");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ОснованияУвольненияПользовательские");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ОсновныеСредства");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ПараметрыВыработкиОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Патенты");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ПодразделенияОрганизаций");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Пользователи");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ПоступлениеДопРасходовПрисоединенныеФайлы");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ПоступлениеТоваровУслугПрисоединенныеФайлы");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ПричиныСписанияОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ПрочиеДоходыИРасходы");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("РасходыБудущихПериодов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("РегистрацииВНалоговомОргане");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Резервы");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("РеквизитыКодаДляОплаты");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Склады");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СобытияОС");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Сотрудники");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СпецификацииНоменклатуры");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СпособыДоставки");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СпособыОтраженияЗарплатыВБухУчете");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СпособыОтраженияРасходовПоАмортизации");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СтатусыНалогоплательщиковПоНДФЛ");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СтатьиДвиженияДенежныхСредств");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СтатьиЗатрат");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СтатьиРасходовЗарплата");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СтатьиФинансированияЗарплата");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("СтраныМира");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("Субконто");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ТиповыеОперации");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ТипыЦенНоменклатуры");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ТранспортныеСредства");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("УбыткиПрошлыхЛет");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ФизическиеЛица");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ЦелиРасходаМатериалов");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ЦенныеБумаги");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

	МетаданныеОбъекта = Метаданные.Справочники.Найти("ШтрихкодыУпаковокТоваров");
	Если МетаданныеОбъекта <> Неопределено Тогда
		ДобавитьСтрокуОбъектаСверки(МетаданныеОбъекта, "Справочник", ВыбранныеРанее, СтатусСверки);
	КонецЕсли;

КонецПроцедуры
